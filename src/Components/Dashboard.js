import React, { Component } from "react";

export default class Dashboard extends Component {
  render() {
    return (
      <main>
        <header>
          <h1>Protected</h1>
        </header>
      </main>
    );
  }
}
